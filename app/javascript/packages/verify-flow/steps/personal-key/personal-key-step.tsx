import { PageHeading } from '@18f/identity-components';
import { t } from '@18f/identity-i18n';

function PersonalKeyStep() {
  return <PageHeading>{t('headings.personal_key')}</PageHeading>;
}

export default PersonalKeyStep;

<% self.title = 'Fake Socure Configuration' %>

<h1>Test Socure Configuration</h1>

<div>
  @socure_configurations: <%= @socure_configurations.size %>
</div>

<div class="grid-row grid-cap margin-top-2 flex-align-stretch">
  <div class="tablet:grid-col-4">
    <div class="card height-full">
      <%= form_with url: "/test/fake_socure", method: :post do |form| %>
        <fieldset class="usa-fieldset">
          <legend class="usa-legend">Select a fixture</legend>
          <%=
            form.collection_select(
              :selected_configuration,
              @socure_configurations,
              :name,
              :pretty_name,
              prompt: true,
            )
          %>
        </fieldset>
        <%= submit_tag "Apply" %>
      <% end %>
    </div>
  </div>
</div>

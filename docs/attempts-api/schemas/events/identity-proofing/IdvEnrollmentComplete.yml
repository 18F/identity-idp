description: |
  When the user is identity verified
allOf:
  - $ref: '../shared/EventProperties.yml'
  - type: object
    properties:
      reproof:
        type: boolean
        description: |
          When true, indicates that the user has proofed previously

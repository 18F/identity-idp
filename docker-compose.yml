version: '3'
services:
  web:
    build: .
    volumes:
      - .:/upaya
    ports:
      - "3000:3000"
    environment:
      redis_url: "redis://redis:6379"
      redis_throttle_url: "redis://redis:6379"
      DATABASE_URL: "postgres://postgres@db"
      DATABASE_CLEANER_ALLOW_REMOTE_DATABASE_URL: 'true'
      # This is a BIG timeout, 500s. The worst offender is `DatabaseCleaner`
      database_timeout: '500000'
      database_statement_timeout: '10000'
      DOCKER_DB_HOST: 'db'
      DOCKER_DB_USER: 'postgres'
      TEST_ENV_NUMBER: '2'
      SMTP_HOST: 'mailcatcher'
    depends_on:
      - db
      - redis
      - mailcatcher
  db:
    image: postgres
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
  redis:
    image: redis
  mailcatcher:
    image: rordi/docker-mailcatcher
    container_name: mailcatcher
    ports:
      - 1080:1080

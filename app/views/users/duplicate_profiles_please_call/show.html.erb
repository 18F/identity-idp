<% self.title = t('users.duplicate_profiles_please_call.heading') %>

<%= render ADS::FormPageComponent.new(
      title: t('users.duplicate_profiles_please_call.heading'),
    ) do |page| %>
  <% page.with_body do %>
    <div class="ads-copy">
      <%= t(
            'users.duplicate_profiles_please_call.error_details_html',
            contact_number: IdentityConfig.store.idv_contact_phone_number,
            help_url: MarketingSite.help_url,
          ) %>
    </div>
  <% end %>
<% end %>

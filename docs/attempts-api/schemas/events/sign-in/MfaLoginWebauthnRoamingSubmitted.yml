description: During a login attempt, the user uses a security key for MFA.
allOf:
  - $ref: '../shared/EventProperties.yml'
  - type: object
    properties:
      success:
        type: boolean
        description: |
          Indicates whether the security key was authenticated successfully for MFA




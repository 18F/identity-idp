#!/usr/bin/env ruby
require 'json'

STDOUT.sync = true

while line = gets
  begin
    puts JSON.pretty_generate(JSON.parse(line))
  rescue
    # handle non-JSON logs, such as when booting up
    puts line
  end
end

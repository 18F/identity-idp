apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "identity-idp.fullname" . }}-test-connection"
  labels:
    {{- include "identity-idp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "identity-idp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

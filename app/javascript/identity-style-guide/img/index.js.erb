<% images_glob = Rails.application.root.join('app', 'javascript', 'identity-style-guide', 'img', '**', '*.{png,svg,jpg,gif}') %>
<% Dir.glob(images_glob).each do |image| %>
  import '<%= image %>';
<% end %>


<% if IdentityConfig.store.updated_account_reset_content %>
  <p class="lead">
    <%= t('user_mailer.account_reset_request.info_html', app_name: link_to(APP_NAME, IdentityConfig.store.mailer_domain_name, class: 'gray'), waiting_period: @account_reset_deletion_period_interval) %>
  </p>

  <p class="lead">
    <%= t('user_mailer.account_reset_request.cancel', app_name: APP_NAME) %>
  </p>
<% else %>
  <p class="lead">
    <%= t('user_mailer.account_reset_request.intro_html', app_name: link_to(APP_NAME, IdentityConfig.store.mailer_domain_name, class: 'gray'), waiting_period: @account_reset_deletion_period_interval) %>
  </p>

  <p class="lead">
    <%= t('user_mailer.account_reset_request.cancel', app_name: APP_NAME) %>
  </p>
<% end %>

<table class="spacer">
  <tbody>
    <tr>
      <td class="s10" height="10px">
        &nbsp;
      </td>
    </tr>
  </tbody>
</table>

<table class="hr">
  <tr>
    <th>
      &nbsp;
    </th>
  </tr>
</table>

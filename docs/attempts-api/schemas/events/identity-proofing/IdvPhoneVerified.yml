description: |
  The user's IdV phone number is assessed
allOf:
  - $ref: '../shared/EventProperties.yml'
  - type: object
    properties:
      phone_number:
        type: string
      failure_reason:
        type: object
        description: |
          An OPTIONAL object. An associative array of attributes and errors if success is false
        properties:
          phone:
            type: array
            description: An OPTIONAL key that describes errors with the phone number
            items:
              type: string
              enum:
                - The phone number could not be verified.
      success:
        type: boolean
        description: |
          Indicates whether the user’s phone was successfully verified.

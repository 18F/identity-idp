apiVersion: v1
kind: Service
metadata:
  name: {{ include "identity_idp.fullname" . }}
  labels:
    {{- include "identity_idp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "identity_idp.selectorLabels" . | nindent 4 }}

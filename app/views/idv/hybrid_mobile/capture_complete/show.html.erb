<% self.title = t('doc_auth.headings.capture_complete_ads') %>

<%= render ADS::FormPageComponent.new(
      title: t('doc_auth.headings.capture_complete_ads'),
      subtitle: t('doc_auth.instructions.switch_back_continue'),
    ) %>

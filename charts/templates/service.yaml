apiVersion: v1
kind: Service
metadata:
  name: {{ include "identity-idp.fullname" . }}
  labels:
    {{- include "identity-idp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "identity-idp.selectorLabels" . | nindent 4 }}

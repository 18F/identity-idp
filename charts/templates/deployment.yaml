apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "identity_idp.fullname" . }}
  labels:
    {{- include "identity_idp.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "identity_idp.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "identity_idp.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        ports:
        - name: http
          containerPort: {{ .Values.service.port }}
          protocol: TCP

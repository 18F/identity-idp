<%= content_tag(
      :button,
      **tag_options,
      class: css_class,
    ) do %> 
  <%= content %>
  <%= image_tag(asset_url(logo_path), width: width, height: height, alt: t('components.login_button.image_alt'), class: 'display-inline text-middle margin-left-1 margin-top-neg-05') %>
<% end %>
---
en:
  activerecord:
    errors:
      models:
        app_setting:
          attributes:
            value:
              cannot_disable_2fa_in_prod: Two-factor authentication cannot be disabled
                in production
              invalid: Value must be '1' or '0'

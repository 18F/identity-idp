description: During a login attempt, the user uses face or touch unlock for MFA.
allOf:
  - $ref: '../shared/EventProperties.yml'
  - type: object
    properties:
      success:
        type: boolean
        description: |
          Indicates whether the entered code matched an expected code




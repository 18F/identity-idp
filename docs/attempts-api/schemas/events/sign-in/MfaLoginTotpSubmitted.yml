description: During a login attempt, the user enters a TOTP code.
allOf:
  - $ref: '../shared/EventProperties.yml'
  - type: object
    properties:
      success:
        type: boolean
        description: |
          Indicates whether the code entered matched what was sent


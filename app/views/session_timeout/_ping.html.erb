<%= render(
      partial: 'session_timeout/warning',
      locals: {
        timeout:,
        warning_offset:,
        timeout_url: new_user_session_url(timeout: :session, request_id: sp_session[:request_id]),
        modal_presenter: modal,
      },
    ) %>

<% if content.present? %>
  <%= render AlertComponent.new(
        type: :error,
        message: content,
        dismissible: false,
        action: {
          label: t('vendor_outage.get_updates'),
          url: StatusPage.base_url,
        },
      ) %>
<% end %>

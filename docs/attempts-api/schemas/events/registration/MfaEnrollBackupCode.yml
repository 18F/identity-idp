description: The user has selected a set of backup codes for MFA.
allOf:
  - $ref: '../shared/EventProperties.yml'
  - type: object
    properties:
      success:
        type: boolean
        description: |
          Indicates whether indicates the backup codes were successfully generated

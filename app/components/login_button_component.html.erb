<%= content_tag(
      :button,
      **tag_options,
      class: css_class,
    ) do %> 
  <%= t('components.login_button.sign_in_html', app_name: content_tag(:span, APP_NAME, class: logo_class)) %>
<% end %>
<%= render AlertComponent.new(
      type: :neutral,
      message: t(
        'account.index.continue_to_service_provider_prompt',
        service_provider: presenter.sp_name,
      ),
      action: {
        label: t(
          'account.index.continue_to_service_provider',
          service_provider: presenter.sp_name,
        ),
        url: presenter.sp_session_request_url,
      },
    ) %>

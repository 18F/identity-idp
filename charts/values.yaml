# Default values for identity_idp.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: XXX.dkr.ecr.region.amazonaws.com/identity-idp/review
  # Overrides the image tag whose default is the chart appVersion.
  tag: XXX

nameOverride: ""
fullnameOverride: ""

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: false
  className: alb
  acmCertArn: arn:aws:acm:us-west-2:894947205914:certificate/a696b1c7-00cf-4188-8361-bca780b7fc49
  albGroupName: review-app
  annotations: {}
  hosts: []
    # - host: chart-example.local
    #   paths:
    #     - path: /
    #       pathType: Prefix

<%#
locals:
* steps: Current step.
* current_step: Whether user is awaiting verification by mail. Defaults to `false`.
%>
<%= render(
  'shared/step_indicator',
  class: 'mobile-lg:margin-x-neg-6 mobile-lg:margin-top-neg-4 margin-bottom-5',
  steps: steps,
  current_step: current_step,
) %>

description: |
  When the user enters their Login.gov password during the identity proofing flow. 
allOf:
  - $ref: '../shared/EventProperties.yml'
  - type: object
    properties:
      success:
        type: boolean
        description: |
          Indicates whether the password entered matches the user’s password

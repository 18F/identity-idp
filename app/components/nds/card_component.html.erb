<%= action.call(
      **wrapper_options,
      class: css_class,
    ) do %>
  <div class="card__inner">
    <div class="card__body">
      <%= content %>
    </div>
    <% if trailing? %>
      <div class="card__trailing">
        <%= trailing %>
      </div>
    <% end %>
  </div>
<% end %>

<!-- NOTE: you will need USWDS in addition to the styles below -->
<%= raw('<style>' + css + '</style>') %>
<%= content_tag(
      :button,
      **tag_options,
      class: css_class,
    ) do %> 
  Sign in with <%= content_tag(:span, APP_NAME, class: 'login-button__logo') %>
<% end %>

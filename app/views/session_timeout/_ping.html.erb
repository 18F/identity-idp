<%= tag.div id: 'session-timeout-cntnr',
            data: {
              timeout_url: timeout_url,
              warning: warning,
              start: start,
              frequency: frequency,
              warning_info_html: render(
                partial: 'session_timeout/warning',
                locals: { modal: modal },
              ),
            } %>

<%= javascript_packs_tag_once 'session-timeout-ping' %>

description: |
  When the user selects the method (mobile or desktop) for uploading the documentation required for identity proofing. 
allOf:
  - $ref: '../shared/EventProperties.yml'
  - type: object
    properties:
      upload_method:
        type: string
        enum: 
          - mobile
          - desktop

<%= content_tag(
      :'lg-nds-password-strength',
      'input-id': input_id,
      'minimum-length': minimum_length,
      'forbidden-passwords': forbidden_passwords.to_json,
      **tag_options.except(:class, :hidden, :data),
      class: css_class,
      hidden: true,
      data: { open: 'false', **tag_options.fetch(:data, {}).to_h },
    ) do %>
  <div class="password-strength__inner">
    <div class="password-strength__row">
      <div class="password-strength__track" aria-hidden="true">
        <span class="password-strength__bar"></span>
      </div>
      <p class="password-strength__feedback" id="<%= feedback_id %>" aria-live="polite"></p>
    </div>
  </div>
<% end %>
